Running shiny app
```{r}
library(shiny)

ui <- navbarPage("The Interaction of Mental Health and Positionality in Literature",
  tabPanel("Project Overview"),
  tabPanel("Positionality of Author"),
  tabPanel("Positionality of Protagonist")
)

server <- function(input, output, session) {
  
}

shinyApp(ui, server)
```